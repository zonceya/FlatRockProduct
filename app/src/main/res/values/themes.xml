<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FlatRockProduct" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FlatRockProduct" parent="Base.Theme.FlatRockProduct">
        <!-- Ensure action bar is included -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <style name="Theme.FlatRockProduct.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item> <!-- Change to false -->
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.FlatRockProduct.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.FlatRockProduct.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
